{% extends "base.html" %}
{% block content %}

<div class="container">
    <div class="row my-3">
        <div class="col-5">
            <h4>{{ user }}'s 마이페이지</h4>
        </div>

    </div>
    <form action="{% url 'common:mypage' %}" enctype="multipart/form-data" method="POST">
        {% csrf_token %}


        <div class="row">
            <div class="col-xs-6 col-md-3">
                {% if profile_user.profile.profile_photo %}
                <img class="round" src="{{profile_user.profile.profile_photo.url}}" width="200"> <br>
                {% else %}
                <img class="round" src="" width="200">
                <br>
                {% endif %}
            </div>

            <div class="col-xs-6 col-md-3">
                {% if user.nickname %}
                <h2>{{user.nickname}}</h2>
                {% endif %}
                {% if user == user %}
                <!-- 로그인한 사용자와 profile_user(프로필 소유자)가 일치할 때만 프로필 편집이 가능하도록 함 -->
                <a href="#">
                    <button type="button" class="btn btn-outline-secondary">프로필 편집</button>
                </a>
                {% endif %}
                <h4> username: {{ user }} </h4>
                <h4> name: {{ user.first_name }}{{ user.last_name }}</h4>
                {% if profile.user == author.user %}
                <h4> birth: {{ profile.birthdate }}</h4>
                <h4> MBTI: {{ profile.mbti }}</h4>
                {% endif %}
            </div>
        </div>
    </form>
</div>




<!-- article 끌어오기 참조용 -->
<section class="vh-100" style="background-color: white;">
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-md-12 col-xl-8">

        <div class="card" style="border-radius: 15px;">

          <div class="card-body text-center">
{#            {% if request.user == profile.author %}#}
{#            <div class="bnt col-1 float-right">#}
{#            <a onclick="location.href='{% url 'common/mypage.html' author.id %}'">#}
{#                <img src="{% static 'icon/pencil.svg' %}">#}
{#                </a>#}
{#            </div>#}
{#                <fiv#}
{#            {% endif %}#}


            <div class="mt-3 mb-4">
              <img src="{{ profile.images.url }}"
                class="rounded-circle img-fluid" style="width: 100px;" />
            </div>

            <h4 class="mb-2">{{ profile.author }}</h4>
            <p class="text-muted mb-4">{{ article.subject}} <span class="mx-2">|</span> <a
                ></a></p>
            <div class="mb-2">
              운동 파트너 요구사항 테스트
          </div>
            <div class="mb-4 pb-2">
                 <div  class="btn btn-outline-primary btn-floating">
                    {{ profile.birthdate }}
                  </div>
                  <div  class="btn btn-outline-primary btn-floating">
                    {%  include 'workout_mach.html' %}
                  </div>
              <div  class="btn btn-outline-primary btn-floating">
                {{ profile.mbti }}
              </div>
            </div>


              <!--오픈챗-->

            <div class="d-flex justify-content-between text-center mt-5 mb-2">
              <div class="col-4">
                <p class="mb-2 h7">{{ article.sport_date }}</p>
                <p class="text-muted mb-0">운동 날짜</p>
              </div>
              <div class="col-4">
                <p class="mb-2 h7">{{ article.voter.count }}</p>
                <p class="text-muted mb-0">추천수</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

{% endblock %}
